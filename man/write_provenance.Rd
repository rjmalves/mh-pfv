% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/provenance.r
\name{write_provenance}
\alias{write_provenance}
\title{Escreve Registro de Proveniencia em JSON}
\usage{
write_provenance(provenance, output_dir)
}
\arguments{
\item{provenance}{lista de proveniencia (preferencialmente finalizada)}

\item{output_dir}{character, diretorio de saida}
}
\value{
caminho do arquivo escrito (invisivelmente)
}
\description{
Serializa o registro de proveniencia como JSON e escreve em disco.
Converte timestamps \code{POSIXct} para strings ISO 8601 antes da
serializacao. Envolvido em \code{tryCatch} para nunca interromper o
pipeline -- falhas de escrita geram apenas um aviso no log.
}
\examples{
\dontrun{
cfg <- list(ids_usinas = "USI1", janela = c("2025-07-01", "2025-09-30"))
prov <- create_provenance(cfg, "train", FALSE)
prov <- finalize_provenance(prov, "completed")
write_provenance(prov, tempdir())
}

}
