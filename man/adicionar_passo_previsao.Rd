% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.r
\name{adicionar_passo_previsao}
\alias{adicionar_passo_previsao}
\title{Adiciona a coluna passo_prev com base na diferenca entre datas de rodada e previsao}
\usage{
adicionar_passo_previsao(dt_irrad_prev_filt)
}
\arguments{
\item{dt_irrad_prev_filt}{Um data.table contendo pelo menos as colunas
\code{data_hora_rodada} e \code{data_hora_previsao}. As colunas podem estar em
qualquer formato que possa ser convertido para POSIXct.}
}
\value{
O mesmo data.table de entrada, com uma nova coluna \code{passo_prev}
indicando o passo de previsao em dias (exemplo: "D+0", "D+1", etc).
}
\description{
Esta funcao calcula o passo de previsao (em dias) entre as colunas
\code{data_hora_rodada} e \code{data_hora_previsao} e adiciona a coluna \code{passo_prev}
no formato "D+N", onde N e o numero inteiro de dias de diferenca.
}
\details{
A funcao converte as colunas \code{data_hora_rodada} e \code{data_hora_previsao} para
o tipo POSIXct, garantindo consistencia no calculo de diferencas de datas.
Em seguida, calcula a diferenca de dias inteiros entre as duas colunas e
gera a string do passo de previsao.
}
\examples{
library(data.table)
dt <- data.table(
    data_hora_rodada = as.POSIXct(c("2025-08-03 00:00:00", "2025-08-03 00:00:00")),
    data_hora_previsao = as.POSIXct(c("2025-08-03 01:00:00", "2025-08-04 01:00:00"))
)
dt <- adicionar_passo_previsao(dt)
}
