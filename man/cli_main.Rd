% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cli.r
\name{cli_main}
\alias{cli_main}
\title{Funcao Principal de Linha de Comando}
\usage{
cli_main(datadir = "./data", parallel = FALSE, resume = FALSE, workers = NULL)
}
\arguments{
\item{datadir}{caminho para o diretorio de dados de entrada}

\item{parallel}{logico; habilita processamento paralelo de usinas.
Quando \code{FALSE} (padrao), a variavel \code{MHPFV_PARALLEL} e consultada}

\item{resume}{logico; habilita retomada do pipeline a partir do ultimo
checkpoint. Quando \code{FALSE} (padrao), a variavel \code{MHPFV_RESUME} e consultada}

\item{workers}{inteiro positivo com o numero de workers paralelos, ou \code{NULL}
para auto-detect. Quando \code{NULL}, a variavel \code{MHPFV_WORKERS} e consultada}
}
\value{
\code{0L} de forma invisivel em caso de sucesso; levanta erro em caso de falha
}
\description{
Ponto de entrada principal do pacote \strong{mhpfv}. Carrega configuracao,
valida parametros e despacha para o modo de execucao adequado
(treinamento ou previsao).
}
\details{
A resolucao de prioridade de cada parametro segue a ordem:
\strong{argumento explicito} > \strong{variavel de ambiente} > \strong{valor padrao}.\tabular{llll}{
   Parametro \tab Flag CLI \tab Variavel de Ambiente \tab Padrao \cr
   \code{parallel} \tab \code{--parallel} \tab \code{MHPFV_PARALLEL} \tab \code{FALSE} \cr
   \code{resume} \tab \code{--resume} \tab \code{MHPFV_RESUME} \tab \code{FALSE} \cr
   \code{workers} \tab \verb{--workers N} \tab \code{MHPFV_WORKERS} \tab \code{NULL} \cr
}
}
\seealso{
\code{\link[=train_main]{train_main()}}, \code{\link[=predict_main]{predict_main()}}, \code{\link[=parse_config]{parse_config()}}, \code{\link[=get_parser]{get_parser()}},
\code{\link[=setup_parallel_plan]{setup_parallel_plan()}}
}
