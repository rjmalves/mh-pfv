% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/consistencia-dados.r
\name{checa_valores_congelados}
\alias{checa_valores_congelados}
\title{Remove Valores Congelados da Geracao Observada}
\usage{
checa_valores_congelados(dt, v_n_valores = c(5, 8), v_limiar = c(0.01, 0.1))
}
\arguments{
\item{dt}{\code{data.table} contendo as colunas \code{id_fonte_observacao}, \code{data_hora_observacao} e \code{valor}.
Representa os dados de geracao observada de uma ou mais fontes.}

\item{v_n_valores}{Vetor numerico de tamanho \code{n} indicando os tamanhos das janelas para deteccao de repeticao.
Valor defaut: \code{c(5, 8)}.
Pode ser de qualquer dimensao, porem "limiar" deve ter a mesma dimensao.}

\item{v_limiar}{Vetor numerico de tamanho \code{n} indicando os limiares maximos de variacao dentro da janela.
Valor defaut: \code{c(0.01, 0.1)}.}
}
\value{
O mesmo \code{data.table}, com a coluna \code{valor} ajustada: valores considerados congelados
sao substituidos por \code{NA}.
}
\description{
Detecta e trata valores "congelados" (sem variacao) nos dados de geracao observada,
aplicando filtros sequenciais para cada fonte de observacao com base em limiares de repeticao.
}
\details{
A funcao aplica a funcao \code{remove_congelados()} duas vezes por grupo (\code{id_fonte_observacao}),
com diferentes parametros de janela e limiar, permitindo uma filtragem mais robusta de dados
suspeitos de congelamento.
}
\seealso{
\code{\link[=remove_congelados]{remove_congelados()}}
}
